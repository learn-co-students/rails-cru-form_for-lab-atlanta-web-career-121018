<h1><%= @song.name %></h1>

<h2><%= link_to @song.artist.name, artist_path(@song.artist) %></h2>

<h3><%= link_to @song.genre.name, genre_path(@song.genre) %></h3>
<br>

<p><%= link_to "Edit", edit_song_path(@song) %></p>

<p><%= link_to "Back", songs_path %></p>
