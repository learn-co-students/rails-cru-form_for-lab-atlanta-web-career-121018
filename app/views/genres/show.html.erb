<h1><%= @genre.name %></h1>

<h2>Songs</h2>

<ul>
  <% @genre.songs.each do |song| %>
    <li><%= link_to song.artist.name + " - " + song.name, song_path(song) %></li>
  <% end %>
</ul>
<br>

<p><%= link_to "Edit", edit_genre_path(@genre) %></p>

<p><%= link_to "Back", genres_path %></p>
